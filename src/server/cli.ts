#!/usr/bin/env node
// Placeholder for slice 01. The CLI entrypoint lands in a subsequent commit on
// this branch. Exiting non-zero on import makes the spawn-based tests fail
// loudly until the implementation arrives.
process.stderr.write("sidebar cli not yet implemented\n");
process.exit(1);
